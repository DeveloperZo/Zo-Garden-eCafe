# Dependencies
**/node_modules/

# Quartz outputs
garden/public/
garden/.quartz-cache/
garden/prof/
garden/tsconfig.tsbuildinfo

# Build outputs (CRA / generic)
**/build/
**/dist/

# OS / editor / local-only tool state
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/
.obsidian/
.claude/settings.local.json

# zowilliams.github.io is now subsumed under githubpage/ (its own .git was
# removed; we're not pushing back to the source repo).
